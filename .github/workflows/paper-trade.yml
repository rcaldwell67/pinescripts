name: Deprecated Paper Trade Path

# Deprecated compatibility shim.
# The active CLM workflow now lives at .github/workflows/paper-trade-clm.yml.
# This file remains valid only to avoid stale editor diagnostics and to
# make the rename explicit for anyone opening the old path.
on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow rename
        run: |
          echo "Deprecated workflow path."
          echo "Use .github/workflows/paper-trade-clm.yml for CLM paper trading."
          echo "Use .github/workflows/paper-trade-btcusd.yml for BTC-USD paper trading."
