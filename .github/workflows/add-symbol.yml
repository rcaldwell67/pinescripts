name: Add Symbol to Database

on:
  issues:
    types: [opened, edited]

jobs:
  add-symbol:
    if: ${{ startsWith(github.event.issue.title, 'Symbol Addition Request:') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Extract symbol from issue
        id: extract
        run: |
          SYMBOL=$(echo "${{ github.event.issue.title }}" | sed 's/Symbol Addition Request: //')
          echo "symbol=$SYMBOL" >> $GITHUB_OUTPUT

      - name: Validate symbol with Alpaca
        id: validate
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_API_SECRET: ${{ secrets.ALPACA_API_SECRET }}
        run: |
          SYMBOL="${{ steps.extract.outputs.symbol }}"
          if [ -z "$ALPACA_API_KEY" ] || [ -z "$ALPACA_API_SECRET" ]; then
            echo "Alpaca API keys not set. Skipping validation."
            exit 1
          fi
          # Validate symbol exists on Alpaca
          curl -s -H "APCA-API-KEY-ID: $ALPACA_API_KEY" -H "APCA-API-SECRET-KEY: $ALPACA_API_SECRET" \
            "https://paper-api.alpaca.markets/v2/assets/$SYMBOL" | grep 'symbol' || exit 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Add symbol to database
        run: |
          python3 backend/data/add_symbol_to_db.py "${{ steps.extract.outputs.symbol }}"

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Add symbol ${{ steps.extract.outputs.symbol }} to tradingcopilot.db via issue request"
          file_pattern: "docs/data/tradingcopilot.db"
          branch: main
